# 🎬 Movie Ranking Project

This project is a simple exercise to practice basic HTML.

## 🎯 Goal

The goal is to create a simple HTML file that ranks your favorite movies. You will need to use different HTML elements to structure the content.

## 📝 Instructions

1.  Open the `index.html` file.
2.  Add the necessary HTML code to create a movie ranking page.
3.  The final result should look like the `goal.png` image.

### HTML Elements to Use

You should use the following HTML elements:

*   `<h1>` for the main title.
*   `<h2>` for the subtitle.
*   `<h3>` for each movie title.
*   `<p>` for the movie descriptions.
*   `<hr>` to create a horizontal line.

Good luck! 🚀
